version: '3'
services:
  web:
    build: .
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html
    environment:
      - DB_HOST=127.0.0.1
      - DB_NAME=rep_db
      - DB_USER=root
      - DB_PASS=
    # depends_on:
    #   - db

#   db:
#     image: mysql:5.7
#     ports:
#       - "3306:3306"
#     environment:
#       MYSQL_ROOT_PASSWORD: ${DB_PASS}
#       MYSQL_DATABASE: ${DB_NAME}
#       MYSQL_USER: ${DB_USER}
#       MYSQL_PASSWORD: ${DB_PASS}
#     volumes:
#       - db_data:/var/lib/mysql

# volumes:
#   db_data:
